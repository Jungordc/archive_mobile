/** @format */

import { View, Text } from "react-native";
import React from "react";

export type ReaderDocsProps = {};
const ReaderDocs: React.FC<ReaderDocsProps> = ({ ...props }) => {
    return (
        <View>
            <Text>ReaderDocs</Text>
        </View>
    );
};

export default ReaderDocs;
