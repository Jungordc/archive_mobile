/** @format */

import { View, Text } from "react-native";
import React from "react";

export type SearchDetailProps = {};

const SearchDetail: React.FC<SearchDetailProps> = (props) => {
    return (
        <View>
            <Text>SearchDetail</Text>
        </View>
    );
};

export default SearchDetail;
