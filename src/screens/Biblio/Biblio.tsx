/** @format */

import { View, Text } from "react-native";
import React from "react";

export type BiblioProps = {};

const Biblio: React.FC<BiblioProps> = ({}) => {
    return (
        <View>
            <Text>Biblio</Text>
        </View>
    );
};

export default Biblio;
