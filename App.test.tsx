/** @format */

import "react-native";
import * as React from "react";
import * as render from "react-test-renderer";
import App from "./App";

it("Test App without crashing", () => {
    // const rendered = render.create(<App />).toJSON();
    // expect(rendered).toMatchSnapshot();
});
